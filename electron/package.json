{
  "name": "nebula-electron",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently \"cd ../frontend && npm install && npm run dev\" \"cross-env NODE_ENV=dev electron .\"",
    "start": "cross-env NODE_ENV=prod electron ."
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3"
  }
}